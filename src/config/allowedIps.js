// Lista de IPs permitidas para conectarse al servidor
const ALLOWED_IPS = [
  '127.0.0.1',        // localhost
  '192.168.0.103'   // Reemplaza con la IP de la otra computadora
];

module.exports = ALLOWED_IPS;